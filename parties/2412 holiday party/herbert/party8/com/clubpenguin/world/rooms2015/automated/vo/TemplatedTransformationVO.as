//Created by Action Script Viewer - https://www.buraks.com/asv
    class com.clubpenguin.world.rooms2015.automated.vo.TemplatedTransformationVO
    {
        var _transformName, _transformToken, _transformId, _unlockDay, _unlockTaskNumber, _gothere, _isMember;
        function TemplatedTransformationVO () {
        }
        function set transformName(string) {
            _transformName = string;
            //return(transformName);
        }
        function get transformName() {
            return(_transformName);
        }
        function set transformToken(type) {
            _transformToken = type;
            //return(transformToken);
        }
        function get transformToken() {
            return(_transformToken);
        }
        function set transformId(num) {
            _transformId = num;
            //return(transformId);
        }
        function get transformId() {
            return(_transformId);
        }
        function set unlockDay(date) {
            _unlockDay = date;
            //return(unlockDay);
        }
        function get unlockDay() {
            return(_unlockDay);
        }
        function set unlockTaskNumber(num) {
            _unlockTaskNumber = num;
            //return(unlockTaskNumber);
        }
        function get unlockTaskNumber() {
            return(_unlockTaskNumber);
        }
        function set gothere(string) {
            _gothere = string;
            //return(gothere);
        }
        function get gothere() {
            return(_gothere);
        }
        function set isMember(bool) {
            _isMember = bool;
            //return(isMember);
        }
        function get isMember() {
            return(_isMember);
        }
    }
